# Gateway Application Configuration
# Database Configuration (Master/Default)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rms
DB_USERNAME=rms
DB_PASSWORD=rms

# Keycloak Configuration - GATEWAY REALM
KEYCLOAK_HOST=rmsauth.atparui.com
KEYCLOAK_REALM=gateway
KEYCLOAK_CLIENT_ID=gateway-web
KEYCLOAK_CLIENT_SECRET=M5nP8qR2sT6uV9wX1yZ3aC4dE7fG0h
KEYCLOAK_BASE_URL=https://rmsauth.atparui.com

# Application Configuration
APP_PORT=8082
APP_PROFILE=dev
SPRING_PROFILES_ACTIVE=dev

# Consul Configuration
CONSUL_ENABLED=true
CONSUL_HOST=localhost
CONSUL_PORT=8500

# Elasticsearch Configuration
ELASTICSEARCH_ENABLED=false
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200

# Multi-tenancy Configuration - GATEWAY AS DEFAULT
MULTITENANCY_ENABLED=true
MULTITENANCY_DEFAULT_TENANT=gateway
MULTITENANCY_TENANT_HEADER=X-Tenant-ID

# Tenant Configuration - GATEWAY CREDENTIALS
TENANT_CLIENT_ID=gateway-web
TENANT_CLIENT_SECRET=M5nP8qR2sT6uV9wX1yZ3aC4dE7fG0h

# Logging Configuration
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_APP=DEBUG

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:8100,https://localhost:8100,http://localhost:9000,https://localhost:9000