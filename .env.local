# Local Development Configuration for IntelliJ
# Environment Variables (Single Line for IntelliJ): TENANT_DB_HOST=localhost;TENANT_DB_PORT=5432;TENANT_DB_NAME=tenant_db;TENANT_DB_USERNAME=tenant_db;TENANT_DB_PASSWORD=tenant_db;DB_HOST=localhost;DB_PORT=5432;DB_NAME=rms;DB_USERNAME=rms;DB_PASSWORD=rms

# Tenant Master Database Configuration
TENANT_DB_HOST=localhost
TENANT_DB_PORT=5432
TENANT_DB_NAME=tenant_db
TENANT_DB_USERNAME=tenant_db
TENANT_DB_PASSWORD=tenant_db

# Database Configuration (Local PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rms
DB_USERNAME=rms
DB_PASSWORD=rms

# Tenant Database Configuration
DB_TENANT1_NAME=rms_tenant1
DB_TENANT2_NAME=rms_tenant2

# Keycloak Configuration (Cloud)
KEYCLOAK_HOST=rmsauth.atparui.com
KEYCLOAK_REALM=gateway
KEYCLOAK_CLIENT_ID=gateway-web
KEYCLOAK_CLIENT_SECRET=M5nP8qR2sT6uV9wX1yZ3aC4dE7fG0h

# Application Configuration
APP_PORT=8081
APP_PROFILE=dev
SPRING_PROFILES_ACTIVE=dev

# Consul Configuration (Enabled)
CONSUL_ENABLED=true
CONSUL_HOST=localhost
CONSUL_PORT=8500

# Elasticsearch Configuration (Enabled)
ELASTICSEARCH_ENABLED=false
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200

# Multi-tenancy Configuration
MULTITENANCY_ENABLED=true
MULTITENANCY_DEFAULT_TENANT=gateway
MULTITENANCY_TENANT_HEADER=X-Tenant-ID

# Logging Configuration
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_APP=DEBUG
LOG_LEVEL_SQL=DEBUG

# CORS Configuration (Local Development)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200,http://localhost:8100,http://localhost:9000,http://localhost:9060,http://localhost:9061,http://localhost:9062

# JVM Options for Development
JAVA_OPTS=-Xmx1024m -Xms512m -Dspring.profiles.active=dev

# Debug Configuration
DEBUG_PORT=5005
DEBUG_ENABLED=true

# Docker Compose (Disabled - using external services)
SPRING_DOCKER_COMPOSE_ENABLED=false