# Database Configuration for Docker
DB_HOST=postgresql
DB_PORT=5432
DB_NAME=rms
DB_USERNAME=rms
DB_PASSWORD=

# Tenant Database Configuration
DB_TENANT1_NAME=rms_tenant1
DB_TENANT2_NAME=rms_tenant2

# Keycloak Configuration
KEYCLOAK_HOST=rmsauth.atparui.com
KEYCLOAK_REALM=gateway
KEYCLOAK_CLIENT_ID=gateway-web
KEYCLOAK_CLIENT_SECRET=M5nP8qR2sT6uV9wX1yZ3aC4dE7fG0h

# Application Configuration
APP_PORT=8080
APP_PROFILE=prod

# Consul Configuration
CONSUL_HOST=consul
CONSUL_PORT=8500

# Elasticsearch Configuration
ELASTICSEARCH_HOST=elasticsearch
ELASTICSEARCH_PORT=9200

# Multi-tenancy Configuration
MULTITENANCY_ENABLED=true
MULTITENANCY_DEFAULT_TENANT=default
MULTITENANCY_TENANT_HEADER=X-Tenant-ID

# Logging Configuration
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_APP=INFO

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:8100,https://localhost:8100,http://localhost:9000,https://localhost:9000
