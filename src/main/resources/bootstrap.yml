spring:
  application:
    name: rms
  profiles:
    active: '@spring.profiles.active@'
  docker:
    compose:
      enabled: false
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: false
        format: YAML
        data-key: data
        prefix: config
        default-context: application
        profile-separator: ','
        watch:
          enabled: true
          delay: 1000
      discovery:
        enabled: false
        prefer-ip-address: true
